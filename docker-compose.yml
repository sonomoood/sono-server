version: '3.7'

services:
  mongodb:
      container_name: mongodb
      image: mongo:latest
      ports:
        - "27017:27017"

  mongo_seed:
      build: ./mongo_seed
      depends_on:
        - mongodb
      profiles:
        - seeds